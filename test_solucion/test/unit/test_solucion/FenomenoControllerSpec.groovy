package test_solucion

import grails.test.mixin.TestFor
import spock.lang.Specification

/**
 * See the API for {@link grails.test.mixin.web.ControllerUnitTestMixin} for usage instructions
 */
@TestFor(FenomenoController)
class FenomenoControllerSpec extends Specification {

    /*void 'test index'() {
        when:
        controller.index()

        then:
        response.redirectedUrl == '/fenomeno/index'
    }

    void 'test list'() {
        when:
        controller.list()

        then:
        response.redirectedUrl == '/fenomeno/list'
    }*/
    def setup() {
    }

    def cleanup() {
    }

    void "test something"() {
    }



}
